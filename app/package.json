{
  "name": "BalerDisplay",
  "version": "0.0.1",
  "description": "Baler Display",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bash npm-scripts/build.sh dev",
    "clean": "rm -rf build && rm -rf coverage && find . -name '*.js' -type f -not -path './node_modules' -delete && find . -name '*.js.map' -type f -not -path './node_modules' -delete",
    "tslint": "bash npm-scripts/tslint.sh",
    "tsc": "tsc || exit 0",
    "tsd": "tsd",
    "noop": "echo noop"
  },
  "author": "Doug Conyers",
  "license": "ISC",
  "devDependencies": {
    "angular": "^1.5.0",
    "electron-prebuilt": "^0.36.7",
    "electron-rebuild": "^1.1.3",
    "tslint": "^3.5.0",
    "typescript": "^1.8.2",
    "typings": "^0.6.8"
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "jquery": "^2.2.1",
    "nan": "^2.2.0"
  }
}
